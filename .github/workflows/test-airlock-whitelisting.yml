name: Test Airlock Whitelisting

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      chains:
        description: 'Chains to test (comma-separated: base,base-sepolia,arbitrum,bsc,mainnet,monad-mainnet,robinhood or "all")'
        required: false
        default: 'base,base-sepolia,arbitrum,bsc,mainnet,monad-mainnet,robinhood'
        type: string
      rpc_delay_ms:
        description: 'Delay between RPC calls in ms (increase if rate limited)'
        required: false
        default: '1000'
        type: string
  schedule:
    # Run twice daily at 00:00 and 12:00 UTC to catch any whitelisting issues
    - cron: '0 0,12 * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-whitelisting:
    runs-on: ubuntu-latest
    env:
      ALCHEMY_API_KEY: ${{ secrets.ALCHEMY_API_KEY }}
      # workflow_dispatch can specify chains; automated runs keep a reduced default set
      TEST_CHAINS: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.chains || 'base,base-sepolia,arbitrum,bsc,mainnet,monad-mainnet,robinhood') || 'base,base-sepolia,arbitrum,bsc,mainnet,monad-mainnet,robinhood' }}
      RPC_DELAY_MS: ${{ github.event.inputs.rpc_delay_ms || '1000' }}
      CI: true

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Debug env vars
        run: |
          echo "TEST_CHAINS: $TEST_CHAINS"
          echo "RPC_DELAY_MS: $RPC_DELAY_MS"
          echo "ALCHEMY_API_KEY set: $([[ -n "$ALCHEMY_API_KEY" ]] && echo 'yes' || echo 'no')"

      - name: Run airlock whitelisting tests
        id: test
        run: pnpm test whitelisting
        timeout-minutes: 10
        continue-on-error: true

      - name: Generate test summary
        if: always()
        run: |
          if [ -f test-results.json ]; then
            pnpm tsx scripts/generate-test-summary.ts test-results.json
          else
            echo "## Airlock Whitelisting Test Results" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo ":warning: **Test results JSON not found**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Chains tested:** $TEST_CHAINS" >> $GITHUB_STEP_SUMMARY
            echo "**RPC delay:** ${RPC_DELAY_MS}ms" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results.json
          if-no-files-found: ignore

      - name: Check test outcome
        if: steps.test.outcome == 'failure'
        run: exit 1
